<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodePin - Login</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:ital,wght@0,400;0,600;1,600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="./css/loginStyle.css">
</head>
<body>
    <header>
        <img class="logo" src="images/codepin-logo.jpg" alt="CodePin Logo">
        <ul>
            <li class="header" id="logInButton"><a class="btn blue login header" href="/login" role="button">Login</a></li>
            <li class="header" id="signUpButton"><a class="btn red signup header" href="/signup" role="button">Signup</a>
            </li>
        </ul>
    </header>
    <main>
    <% if (locals.messages.errors) { %>
        <% messages.errors.forEach( el => { %>
            <div class="alert alert-danger"><%= el.msg %></div>
        <% }) %>    
    <% } %>
    <% if (locals.messages.info) { %>
        <% messages.info.forEach( el => { %>
            <div class="alert alert-danger"><%= el.msg %></div>
        <% }) %>    
    <% } %>
    <div class="login">
        <h1 class="login">Login to CodePin</h1>
        <form action="/login" method="POST">
            <input type="text" name="email" placeholder="Email Address" required>
            <input type="password" name="password" placeholder="Password" required>
            <button class="btn login" type="submit" name="commit" value="Login">Login</button>
        </form>
    </div>
    
    <div class="login-help">
        <p class="signup">Need an account? <a href="/signup">Click here to create one</a></p>
    </div>
    </main>
</body>
</html>